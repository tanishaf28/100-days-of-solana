/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type ExtendedClient,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import { getVaultCodec, type Vault, type VaultArgs } from "../accounts";
import {
  getDepositInstructionAsync,
  getWithdrawInstructionAsync,
  parseDepositInstruction,
  parseWithdrawInstruction,
  type DepositAsyncInput,
  type ParsedDepositInstruction,
  type ParsedWithdrawInstruction,
  type WithdrawAsyncInput,
} from "../instructions";
import { findVaultPda } from "../pdas";

export const VAULT_PROGRAM_ADDRESS =
  "GdueoRpuvMEw92rxhoVfJxdQcEoTaZUe15ow69WxPkPf" as Address<"GdueoRpuvMEw92rxhoVfJxdQcEoTaZUe15ow69WxPkPf">;

export enum VaultAccount {
  Vault,
}

export function identifyVaultAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): VaultAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([211, 8, 232, 43, 2, 152, 117, 119]),
      ),
      0,
    )
  ) {
    return VaultAccount.Vault;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "vault" },
  );
}

export enum VaultInstruction {
  Deposit,
  Withdraw,
}

export function identifyVaultInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): VaultInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([242, 35, 198, 137, 82, 225, 242, 182]),
      ),
      0,
    )
  ) {
    return VaultInstruction.Deposit;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([183, 18, 70, 156, 148, 109, 161, 34]),
      ),
      0,
    )
  ) {
    return VaultInstruction.Withdraw;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "vault" },
  );
}

export type ParsedVaultInstruction<
  TProgram extends string = "GdueoRpuvMEw92rxhoVfJxdQcEoTaZUe15ow69WxPkPf",
> =
  | ({
      instructionType: VaultInstruction.Deposit;
    } & ParsedDepositInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.Withdraw;
    } & ParsedWithdrawInstruction<TProgram>);

export function parseVaultInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedVaultInstruction<TProgram> {
  const instructionType = identifyVaultInstruction(instruction);
  switch (instructionType) {
    case VaultInstruction.Deposit: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.Deposit,
        ...parseDepositInstruction(instruction),
      };
    }
    case VaultInstruction.Withdraw: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.Withdraw,
        ...parseWithdrawInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        { instructionType: instructionType as string, programName: "vault" },
      );
  }
}

export type VaultPlugin = {
  accounts: VaultPluginAccounts;
  instructions: VaultPluginInstructions;
  pdas: VaultPluginPdas;
  identifyAccount: typeof identifyVaultAccount;
  identifyInstruction: typeof identifyVaultInstruction;
  parseInstruction: typeof parseVaultInstruction;
};

export type VaultPluginAccounts = {
  vault: ReturnType<typeof getVaultCodec> &
    SelfFetchFunctions<VaultArgs, Vault>;
};

export type VaultPluginInstructions = {
  deposit: (
    input: DepositAsyncInput,
  ) => ReturnType<typeof getDepositInstructionAsync> & SelfPlanAndSendFunctions;
  withdraw: (
    input: WithdrawAsyncInput,
  ) => ReturnType<typeof getWithdrawInstructionAsync> &
    SelfPlanAndSendFunctions;
};

export type VaultPluginPdas = { vault: typeof findVaultPda };

export type VaultPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function vaultProgram() {
  return <T extends VaultPluginRequirements>(
    client: T,
  ): ExtendedClient<T, { vault: VaultPlugin }> => {
    return extendClient(client, {
      vault: <VaultPlugin>{
        accounts: { vault: addSelfFetchFunctions(client, getVaultCodec()) },
        instructions: {
          deposit: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getDepositInstructionAsync(input),
            ),
          withdraw: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getWithdrawInstructionAsync(input),
            ),
        },
        pdas: { vault: findVaultPda },
        identifyAccount: identifyVaultAccount,
        identifyInstruction: identifyVaultInstruction,
        parseInstruction: parseVaultInstruction,
      },
    });
  };
}
